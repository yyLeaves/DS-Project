package com.sociopath.events;

import java.util.ArrayList;
import java.util.LinkedList;
import java.util.Queue;

/**
 * Description:
 *
 * @author Yeyang Liu, S2000549
 * Created on: 2021/5/21 2:10, in project PACKAGE_NAME
 */


//
public class E5MeetCrush {

    /*public static void main(String[] args) {
        int currentStudent = 1;
        ArrayList<Student> students = E0Init.init();
        ArrayList<Student> strangers = Student.getStrangers(students, students.get(currentStudent - 1));

        // according to the relationship given, strangers must > 2

        Student rumorStudent = strangers.get((int) (Math.random() * strangers.size()));

        boolean isSame = true;
        while (isSame) {
            Student crushStudent = strangers.get((int) (Math.random() * strangers.size()));
            if (!crushStudent.equals(rumorStudent)) {
                isSame = false;
            }
        }





    }

    public static boolean isLinked(Student rumor, Student crush) {
        ArrayList<Student> rumorList = new ArrayList<>();
        Queue<Student> visited = new LinkedList<>();

        return false;

    }
*/
}
